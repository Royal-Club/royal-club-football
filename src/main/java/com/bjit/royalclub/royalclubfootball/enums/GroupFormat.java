package com.bjit.royalclub.royalclubfootball.enums;

public enum GroupFormat {
    MANUAL,                // User creates each match
    ROUND_ROBIN_SINGLE,    // Each pair plays once
    ROUND_ROBIN_DOUBLE,    // Each pair plays twice (home & away)
    CUSTOM_MULTIPLE        // User defines encounter count
}
