package com.bjit.royalclub.royalclubfootball.enums;

public enum FixtureFormat {
    SINGLE_ELIMINATION,    // Bracket format (e.g., 8 teams = 4 QF, 2 SF, 1 Final)
    ROUND_ROBIN,           // All teams play each other once
    DOUBLE_ROUND_ROBIN     // All teams play each other twice (home & away)
}

